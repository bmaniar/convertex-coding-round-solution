import React from "react";
import './_topBar.scss';

const TopBar = () => {
    return (
        <header className="d-flex justify-content-center header">
            User Data
        </header>
    );
}
export default TopBar;